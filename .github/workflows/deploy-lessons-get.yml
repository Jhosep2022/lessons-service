name: Deploy lessons-get Lambda (direct)

on:
  push:
    branches: [main, qa]

jobs:
  deploy:
    name: Deploy lessons get
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'dev' || 'qa' }}

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with: { node-version: '20' }

      # Deps (subdir o raíz)
      - name: 📦 Install deps (subdir)
        if: ${{ hashFiles('lessons-service/package.json') != '' }}
        working-directory: lessons-service
        run: |
          if [ -f package-lock.json ]; then npm ci --omit=dev; else npm i --omit=dev; fi
      - name: 📦 Install deps (root)
        if: ${{ hashFiles('lessons-service/package.json') == '' && hashFiles('package.json') != '' }}
        run: |
          if [ -f package-lock.json ]; then npm ci --omit=dev; else npm i --omit=dev; fi

      # Package
      - name: 📦 Package (subdir)
        if: ${{ hashFiles('lessons-service/package.json') != '' }}
        working-directory: lessons-service
        run: zip -r lessons-get.zip src/ node_modules/ package.json package-lock.json || true
      - name: 📦 Package (root)
        if: ${{ hashFiles('lessons-service/package.json') == '' && hashFiles('package.json') != '' }}
        run: zip -r lessons-get.zip src/ node_modules/ package.json package-lock.json || true

      # AWS
      - uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # Deploy
      - name: 🚀 Deploy (subdir)
        if: ${{ hashFiles('lessons-service/package.json') != '' }}
        working-directory: lessons-service
        run: |
          aws lambda update-function-configuration \
            --function-name lessons-service-$STAGE-get \
            --environment "Variables={
                STAGE=${{env.STAGE}},
                COURSES_TABLE_NAME=${{secrets.COURSES_TABLE_NAME}},
                LESSONS_TABLE_NAME=${{secrets.LESSONS_TABLE_NAME}},
                ALLOWED_ORIGINS=${{secrets.ALLOWED_ORIGINS}}
              }"
          aws lambda update-function-code \
            --function-name lessons-service-$STAGE-get \
            --zip-file fileb://lessons-get.zip \
            --publish
      - name: 🚀 Deploy (root)
        if: ${{ hashFiles('lessons-service/package.json') == '' && hashFiles('package.json') != '' }}
        run: |
          aws lambda update-function-configuration \
            --function-name lessons-service-$STAGE-get \
            --environment "Variables={
                STAGE=${{env.STAGE}},
                COURSES_TABLE_NAME=${{secrets.COURSES_TABLE_NAME}},
                LESSONS_TABLE_NAME=${{secrets.LESSONS_TABLE_NAME}},
                ALLOWED_ORIGINS=${{secrets.ALLOWED_ORIGINS}}
              }"
          aws lambda update-function-code \
            --function-name lessons-service-$STAGE-get \
            --zip-file fileb://lessons-get.zip \
            --publish
